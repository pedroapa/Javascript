observação, tem um input que aceita um número no intervalo de 1 a 100.
Tem um select que mostra os valores que estão no vetor, quando insere, ele é 
atualizado
E um botão para finalizar, que faz as operações de encontrar o maior número,
 o menor, a soma de todos e a media.

proteção:
não inserir o mesmo número duas vezes
não inserir nada
inserir um valor invalido (fora da faixa)

primeiro cria a funcionalidade e depois implementar a proteção.
será que na proteção da para criar primeiro o fluxograma antes de criar o 
codigo?

fogo de quando criar o fluxograma levar em conta que é um evento, que só 
será adicionado quando for clicado.

antes de adicionar, saber se o número já existe dentro do vetor.


OBSERVAÇÃO
mesmo que o valor do input se transforme para zero dentro da variavel 'n'. 
No if estou testando o tamanho da propriedade value do objeto 'num'. 

Transformando o value do input para o tipo numero, se o campo estiver vazio 
vai virar zero e neste exercicio não precisaria ver o tamanho do value 
então, já que zero não esta na faixa estimada.



mesmo que o valor do input se transforme para zero dentro da variavel 'n'. No if estou testando o tamanho da propriedade value do objeto 'num'. 

Transformando o value do input para o tipo numero, se o campo estiver vazio
 vai virar zero e neste exercicio não precisaria ver o tamanho do value 
 então, já que zero não esta na faixa estimada.

falta quantos para chegar no 9 e este numero é somado para os dois numeros, 
quando a unidade do numero de cima for maior que o de baixo, não precisa 
fazer isso.

eu iria usar apenas um if simples para as condiçoes de proteção e deixar a 
função desenrolar fazendo as coisas, mas pode acontecer de uma situação não 
pensada e um número invalido for salvo no vetor e dar erro que será mais 
dificil de encontrar. 
Ja que uma vez, se o numero for invalido, vai para um lugar oposto de quando
 é valido, seguindo esse racicionio os dois nunca podem acontecem ao mesmo 
 tempo.
Sendo que sem isso, depois de entrar no if que aparece uma mensagem de erro, iria guardar no vetor o valor invalido porque a execução iria continuar.

5, 3, 4, 3

problema de usar aquela função que ela manda qual index a elemento esta, estão só posso usar ela no teste para ver se o valor é igual a -1

Quando o número não esta dentro, da -1

Mano aonde vai parar o laço de repetição no exercicio?? do jeito que estou 
fazendo, estou fazendo sem. É meio estranho o JS interagir com o laço, 
porque posso usar função somado com evento.

Achar o maior 
menor 
soma de todos 
a media de todos



